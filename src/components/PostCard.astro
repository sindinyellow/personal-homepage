---
interface Props {
  title: string;
  description: string;
  href: string;
  pubDate: Date;
  tags: string[];
  /** 预计阅读分钟数（可选） */
  readingTime?: number;
}

const { title, description, href, pubDate, tags, readingTime } = Astro.props;
const formattedDate = new Intl.DateTimeFormat('zh-CN', {
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
}).format(pubDate);
---

<article class="post-card">
  <a href={href}>
    <time datetime={pubDate.toISOString()}>{formattedDate}</time>
    {readingTime && <span class="reading-time">· {readingTime} 分钟阅读</span>}
    <h3>{title}</h3>
    <p>{description}</p>
    <div class="tag-row">
      {tags.map((tag) => <span>{tag}</span>)}
    </div>
  </a>
</article>
